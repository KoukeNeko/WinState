<Page x:Class="WinState.Views.Pages.MemoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:local="clr-namespace:WinState.Views.Pages"
      Title="MemoryPage"
      d:DataContext="{d:DesignInstance local:MemoryPage, IsDesignTimeCreatable=False}"
      mc:Ignorable="d">

    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="Memory" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
            <TextBlock Text="{Binding ViewModel.RamUsage, StringFormat='Usage: {0:F1}%'}" FontSize="14"/>
            <TextBlock Text="{Binding ViewModel.RamTotalString, StringFormat='Total: {0}'}" FontSize="14"/>
            <TextBlock Text="{Binding ViewModel.RamUsedString, StringFormat='Used: {0}'}" FontSize="14"/>
            <TextBlock Text="{Binding ViewModel.RamFreeString, StringFormat='Free: {0}'}" FontSize="14"/>
            <TextBlock Text="{Binding ViewModel.RamAvailableString, StringFormat='Available: {0}'}" FontSize="14"/>
        </StackPanel>
    </Grid>
</Page>