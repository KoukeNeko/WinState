<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinState.Installer.Pages.SummaryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Ready to install"
                   FontSize="24"
                   FontWeight="SemiBold"/>

        <TextBlock Grid.Row="1"
                   Text="Review your choices, then click Install to begin. Nothing is written to disk until then."
                   TextWrapping="Wrap"
                   Opacity="0.8"/>

        <StackPanel Grid.Row="2" Spacing="14" Margin="0,8,0,0">
            <StackPanel Spacing="2">
                <TextBlock Text="Install location" FontWeight="SemiBold" Opacity="0.7" FontSize="13"/>
                <TextBlock x:Name="PathText" TextWrapping="Wrap"/>
            </StackPanel>

            <StackPanel Spacing="2">
                <TextBlock Text="Optional tasks" FontWeight="SemiBold" Opacity="0.7" FontSize="13"/>
                <TextBlock x:Name="PawnIOText"/>
                <TextBlock x:Name="LaunchText"/>
                <TextBlock x:Name="ShortcutText"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
