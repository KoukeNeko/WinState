<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinState.Installer.Pages.SummaryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:loc="using:WinState.Installer">

    <Grid RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{x:Bind loc:L.SummaryTitle}"
                   FontSize="24"
                   FontWeight="SemiBold"/>

        <TextBlock Grid.Row="1"
                   Text="{x:Bind loc:L.SummaryBody}"
                   TextWrapping="Wrap"
                   Opacity="0.8"/>

        <StackPanel Grid.Row="2" Spacing="14" Margin="0,8,0,0">
            <StackPanel Spacing="2">
                <TextBlock Text="{x:Bind loc:L.OptionsInstallLocation}" FontWeight="SemiBold" Opacity="0.7" FontSize="13"/>
                <TextBlock x:Name="PathText" TextWrapping="Wrap"/>
            </StackPanel>

            <StackPanel Spacing="2">
                <TextBlock Text="{x:Bind loc:L.OptionsOptionalTasks}" FontWeight="SemiBold" Opacity="0.7" FontSize="13"/>
                <TextBlock x:Name="PawnIOText"/>
                <TextBlock x:Name="LaunchText"/>
                <TextBlock x:Name="ShortcutText"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
