<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinState.Installer.Pages.WelcomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:loc="using:WinState.Installer">

    <Grid RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title + language picker on the same row -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       VerticalAlignment="Center"
                       Text="{x:Bind loc:L.Instance.WelcomeTitle, Mode=OneWay}"
                       FontSize="28"
                       FontWeight="SemiBold"/>

            <ComboBox x:Name="LanguageCombo"
                      Grid.Column="1"
                      VerticalAlignment="Center"
                      MinWidth="140"
                      SelectionChanged="LanguageCombo_SelectionChanged">
                <ComboBoxItem Content="English" Tag="en"/>
                <ComboBoxItem Content="繁體中文" Tag="zh"/>
            </ComboBox>
        </Grid>

        <TextBlock Grid.Row="1"
                   Text="{x:Bind loc:L.Instance.WelcomeBody, Mode=OneWay}"
                   TextWrapping="Wrap"
                   Opacity="0.8"/>

        <StackPanel Grid.Row="2"
                    Spacing="8"
                    Margin="0,16,0,0">
            <TextBlock Text="{x:Bind loc:L.Instance.WelcomeWhatItDoes, Mode=OneWay}" FontWeight="SemiBold"/>
            <TextBlock Text="{x:Bind loc:L.Instance.WelcomeBullet1, Mode=OneWay}" TextWrapping="Wrap" Opacity="0.8"/>
            <TextBlock Text="{x:Bind loc:L.Instance.WelcomeBullet2, Mode=OneWay}" TextWrapping="Wrap" Opacity="0.8"/>
            <TextBlock Text="{x:Bind loc:L.Instance.WelcomeBullet3, Mode=OneWay}" TextWrapping="Wrap" Opacity="0.8"/>
            <TextBlock Text="{x:Bind loc:L.Instance.WelcomeBullet4, Mode=OneWay}" TextWrapping="Wrap" Opacity="0.8"/>
        </StackPanel>
    </Grid>
</Page>
