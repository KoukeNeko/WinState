<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinState.Installer.Pages.WelcomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Install WinState"
                   FontSize="28"
                   FontWeight="SemiBold"/>

        <TextBlock Grid.Row="1"
                   Text="WinState is a lightweight Windows system monitor that lives in your tray. This wizard will copy it to your Program Files folder and optionally set up the PawnIO driver and Start-with-Windows."
                   TextWrapping="Wrap"
                   Opacity="0.8"/>

        <StackPanel Grid.Row="2"
                    Spacing="8"
                    Margin="0,16,0,0">
            <TextBlock Text="What it does:" FontWeight="SemiBold"/>
            <TextBlock Text="• Copies WinState to C:\Program Files\WinState (default; you can change this on the next page)." TextWrapping="Wrap" Opacity="0.8"/>
            <TextBlock Text="• Optionally installs the PawnIO kernel driver via WinGet so CPU temperature, voltage, package power and motherboard sensors work." TextWrapping="Wrap" Opacity="0.8"/>
            <TextBlock Text="• Optionally registers a Scheduled Task so WinState starts elevated at logon." TextWrapping="Wrap" Opacity="0.8"/>
            <TextBlock Text="• Adds a Start Menu shortcut and an entry under Apps &amp; features so you can uninstall later." TextWrapping="Wrap" Opacity="0.8"/>
        </StackPanel>
    </Grid>
</Page>
