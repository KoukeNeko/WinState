<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinState.Installer.Pages.OptionsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid RowSpacing="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Setup options"
                   FontSize="24"
                   FontWeight="SemiBold"/>

        <!-- Install location -->
        <StackPanel Grid.Row="1" Spacing="6">
            <TextBlock Text="Install location" FontWeight="SemiBold"/>
            <Grid ColumnSpacing="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="InstallPathBox" Grid.Column="0"/>
                <Button x:Name="BrowseButton" Grid.Column="1" Content="Browse..." Click="BrowseButton_Click"/>
            </Grid>
        </StackPanel>

        <!-- Optional tasks -->
        <StackPanel Grid.Row="2" Spacing="12">
            <TextBlock Text="Optional tasks" FontWeight="SemiBold"/>

            <CheckBox x:Name="PawnIOCheckbox" IsChecked="True">
                <StackPanel>
                    <TextBlock Text="Install the PawnIO driver"/>
                    <TextBlock Text="Required for CPU temperature / voltage / package power and motherboard sensors. Uses winget to install namazso.PawnIO."
                               Opacity="0.7" TextWrapping="Wrap" FontSize="12"/>
                </StackPanel>
            </CheckBox>

            <CheckBox x:Name="LaunchAtLogonCheckbox" IsChecked="True">
                <StackPanel>
                    <TextBlock Text="Launch WinState at logon"/>
                    <TextBlock Text="Registers a Scheduled Task that runs WinState elevated when you sign in."
                               Opacity="0.7" TextWrapping="Wrap" FontSize="12"/>
                </StackPanel>
            </CheckBox>

            <CheckBox x:Name="StartMenuShortcutCheckbox" IsChecked="True">
                <StackPanel>
                    <TextBlock Text="Add a Start Menu shortcut"/>
                </StackPanel>
            </CheckBox>
        </StackPanel>
    </Grid>
</Page>
