<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinState.Installer.Pages.OptionsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:loc="using:WinState.Installer">

    <Grid RowSpacing="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{x:Bind loc:L.OptionsTitle}"
                   FontSize="24"
                   FontWeight="SemiBold"/>

        <!-- Install location -->
        <StackPanel Grid.Row="1" Spacing="6">
            <TextBlock Text="{x:Bind loc:L.OptionsInstallLocation}" FontWeight="SemiBold"/>
            <Grid ColumnSpacing="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="InstallPathBox" Grid.Column="0"/>
                <Button x:Name="BrowseButton" Grid.Column="1" Content="{x:Bind loc:L.OptionsBrowse}" Click="BrowseButton_Click"/>
            </Grid>
        </StackPanel>

        <!-- Optional tasks -->
        <StackPanel Grid.Row="2" Spacing="12">
            <TextBlock Text="{x:Bind loc:L.OptionsOptionalTasks}" FontWeight="SemiBold"/>

            <CheckBox x:Name="PawnIOCheckbox" IsChecked="True">
                <StackPanel>
                    <TextBlock Text="{x:Bind loc:L.OptionsInstallPawnIO}"/>
                    <TextBlock Text="{x:Bind loc:L.OptionsInstallPawnIODesc}"
                               Opacity="0.7" TextWrapping="Wrap" FontSize="12"/>
                </StackPanel>
            </CheckBox>

            <CheckBox x:Name="LaunchAtLogonCheckbox" IsChecked="True">
                <StackPanel>
                    <TextBlock Text="{x:Bind loc:L.OptionsLaunchAtLogon}"/>
                    <TextBlock Text="{x:Bind loc:L.OptionsLaunchAtLogonDesc}"
                               Opacity="0.7" TextWrapping="Wrap" FontSize="12"/>
                </StackPanel>
            </CheckBox>

            <CheckBox x:Name="StartMenuShortcutCheckbox" IsChecked="True">
                <StackPanel>
                    <TextBlock Text="{x:Bind loc:L.OptionsStartMenu}"/>
                </StackPanel>
            </CheckBox>
        </StackPanel>
    </Grid>
</Page>
