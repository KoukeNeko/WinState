<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinState.Installer.Pages.FinishedPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:loc="using:WinState.Installer">

    <Grid RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="HeadingText"
                   Grid.Row="0"
                   Text="{x:Bind loc:L.FinishedTitle}"
                   FontSize="28"
                   FontWeight="SemiBold"/>

        <TextBlock x:Name="BodyText"
                   Grid.Row="1"
                   Text="{x:Bind loc:L.FinishedBody}"
                   TextWrapping="Wrap"
                   Opacity="0.8"/>

        <CheckBox Grid.Row="2"
                  x:Name="LaunchNowCheckbox"
                  Content="{x:Bind loc:L.FinishedLaunchNow}"
                  IsChecked="True"/>
    </Grid>
</Page>
