<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinState.Installer.Pages.FinishedPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="HeadingText"
                   Grid.Row="0"
                   Text="Done"
                   FontSize="28"
                   FontWeight="SemiBold"/>

        <TextBlock x:Name="BodyText"
                   Grid.Row="1"
                   Text="WinState has been installed. It will appear in your system tray; right-click any tray icon for Settings or to exit."
                   TextWrapping="Wrap"
                   Opacity="0.8"/>

        <CheckBox Grid.Row="2"
                  x:Name="LaunchNowCheckbox"
                  Content="Launch WinState now"
                  IsChecked="True"/>
    </Grid>
</Page>
